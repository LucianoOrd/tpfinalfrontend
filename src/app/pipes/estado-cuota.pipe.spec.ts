import { EstadoCuotaPipe } from './estado-cuota.pipe';

describe('EstadoCuotaPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoCuotaPipe();
    expect(pipe).toBeTruthy();
  });
});
